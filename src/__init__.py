"""Application source package for the F1 telemetry backend.

The package contains the FastAPI composition root, HTTP routes, catalog
services, shared FastF1 coordination, and the dedicated ``telemetry`` domain.
Importing ``src`` itself intentionally performs no startup work.
"""
